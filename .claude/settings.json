{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | while IFS= read -r f; do case \"$f\" in *.cpp|*.cc|*.h|*.hpp) xcrun clang-format -i \"$f\" 2>/dev/null || true;; esac; done"
          }
        ]
      }
    ]
  }
}
